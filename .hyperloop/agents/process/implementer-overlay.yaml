apiVersion: hyperloop.io/v1
kind: Agent
metadata:
  name: implementer
guidelines: |
  - No state files: NEVER commit any file under `.hyperloop/state/` — it causes a permanent rebase conflict that cannot be fixed with a fixup commit; the orchestrator replays commits one-by-one so the bad commit conflicts every round.
  - Explicit staging only: Always stage with `git add <specific-file> <specific-file>` — never `git add -A` or `git add .`, which silently include state files.
  - Pre-commit gate: Before every `git commit`, run `git diff --cached --name-only` and abort if any path starts with `.hyperloop/state/`.
  - Check existing work first: Before writing any code, run `git log origin/main --oneline -- <target-files>` to confirm the implementation does not already exist on main from a prior task; do not duplicate delivered work.
  - When work is already done: If all required implementation already exists on origin/main, push a zero-commit branch (do NOT create any commit) and report the finding as plain text in your submission stdout — never write to `.hyperloop/state/` to document this.
  - Run check-staged-state.sh: Execute `bash .hyperloop/checks/check-staged-state.sh` before every commit; it exits 1 if state files are staged and must exit 0 before you proceed.
  - Run check-pre-submit.sh before reporting done: After all commits are made, execute `bash .hyperloop/checks/check-pre-submit.sh`; it verifies both staged files and commit history are free of state files — a clean staged check alone is not sufficient because mistakes can still land state files in a commit.
  - Post-commit history is the final gate: `check-staged-state.sh` only catches files not yet committed; run `bash .hyperloop/checks/check-state-commit-history.sh` after every commit to confirm no state file entered the branch history — this catches errors `check-staged-state.sh` cannot.
