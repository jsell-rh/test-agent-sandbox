{
  "name": "todo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run --config vitest.infra.config.ts",
    "test:watch": "vitest --config vitest.infra.config.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "nuxt": "^3.17.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/better-sqlite3": "^7.6.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
