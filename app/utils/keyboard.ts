/**
 * Shared keyboard key constants.
 *
 * Centralises the `KeyboardEvent.key` values used across multiple components
 * (NewTodoInput, TodoItem) so there are no duplicated magic strings.
 *
 * Named after the standard `KeyboardEvent.key` property values
 * (https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values).
 */

export const KEY_ENTER = 'Enter'
export const KEY_ESCAPE = 'Escape'
