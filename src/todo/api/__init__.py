"""Application Layer — HTTP API for the Todo bounded context.

Translates HTTP requests to domain commands and domain objects to JSON
responses.  Contains no business rules; all invariants live in the Aggregate.
"""
